<a href="{{ $link }}" class="contact-card">

    <div class="contact-icon">

        <i class="{{ $icon }}"></i>

    </div>

    <div class="contact-info">

        <h4>{{ $title }}</h4>

        <p>{{ $value }}</p>

    </div>

</a>